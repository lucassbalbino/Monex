name: Build and Deploy to Hostinger

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build
        env:
          VITE_STRIPE_PUBLIC_KEY: ${{ secrets.VITE_STRIPE_PUBLIC_KEY }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
        run: npm run build

      - name: Deploy to Hostinger via SFTP (SSH key)
        uses: appleboy/scp-action@v0.1.6
        with:
          host: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USERNAME }}
          key: ${{ secrets.SFTP_PRIVATE_KEY }}
          port: ${{ secrets.SFTP_PORT }}
          source: "dist/"
          target: "/public_html"

# Optional: job to deploy Supabase Edge Functions (commented out)
# To enable: add SUPABASE_ACCESS_TOKEN to repo secrets and uncomment the block below
# deploy-supabase-functions:
#   runs-on: ubuntu-latest
#   needs: build-and-deploy
#   steps:
#     - name: Checkout
#       uses: actions/checkout@v4
#     - name: Install supabase cli
#       run: |
#         curl -sL https://deb.nodesource.com/setup_18.x | sudo -E bash -
#         sudo apt-get install -y nodejs
#         npm install -g supabase
#     - name: Deploy functions
#       env:
#         SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
#       run: |
#         supabase login --access-token $SUPABASE_ACCESS_TOKEN
#         supabase functions deploy create-checkout-session --project-ref YOUR_PROJECT_REF